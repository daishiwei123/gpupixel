apply plugin: 'maven-publish'

publishing {
    publications {
        mavenAar(MavenPublication) {
            groupId = 'com.pixpark'
            artifactId = 'gpupixel'
            version = '1.0.7'
            artifact file('gpupixel-release.aar')
        }
    }
}