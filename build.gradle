apply plugin: 'maven-publish'

publishing {
    publications {
        mavenAar(MavenPublication) {
            groupId = 'com.pixpark'
            artifactId = 'gpupixel'
            version = '1.3.1'
            artifact file('gpupixel-release.aar')
        }
    }
}

// 让jitpack能通过默认assemble流程
task assemble {}